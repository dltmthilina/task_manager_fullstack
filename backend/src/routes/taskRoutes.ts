import express from 'express';

const router = express.Router();

router.post('/create-task', ()=>{});
router.get('/tasks', ()=>{});
router.get('/task/:id', );
router.put('/update-task-status/:id, ');
router.put('/update-task/:id', )
router.delete('/delete-task/:id', );
